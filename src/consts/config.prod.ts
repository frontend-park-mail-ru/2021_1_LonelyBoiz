const url = 'lepick.ru';
const backendLocation = 'https://' + url + '/api';
export const websocketLocation = 'wss://' + url + '/api/ws';
export const imageBackendLocation = 'https://' + url + '/images';
export const imageStorageLocation = 'https://' + url + '/image-storage';

export default backendLocation;
