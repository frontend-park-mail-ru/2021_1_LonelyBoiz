<div class="form-list">
    <form {{#if id}}id="{{id}}" {{/if}}>
        {{#each FormList}}
        {{{this}}}
        {{else}}
        Настроек нет
        {{/each}}
        <input type="submit" hidden>
    </form>
</div>
